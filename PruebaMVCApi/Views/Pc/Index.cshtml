@model IEnumerable<Datos.Pc>

@{
    ViewBag.Title = "PcList";
}

<h2>PcList</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Procesador)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Memoria)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Procesador)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Memoria)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
